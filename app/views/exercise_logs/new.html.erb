<div class="container mx-auto max-w-screen-sm px-4 py-4 space-y-4">
  <header class="space-y-1 text-center">
    <h1 class="text-xl font-bold">運動を記録</h1>
    <p class="text-sm text-gray-600">今日の運動を記録しましょう。</p>
  </header>

  <% if @exercise_log.errors.any? %>
    <div class="rounded-lg border border-red-200 bg-red-50 p-3 text-red-700 text-sm">
      入力エラーが <%= @exercise_log.errors.count %> 件あります。各項目の下のメッセージを確認してください。
    </div>
  <% end %>

  <section class="rounded-2xl border shadow-sm p-4 sm:p-6 bg-white ">
    <%= render "form", exercise_log: @exercise_log %>
  </section>

  <div class="pt-2">
    <%= link_to "ホームに戻る", authenticated_root_path,
      class: "mx-auto block w-fit rounded-xl border px-4 py-2 text-sm text-gray-700 hover:bg-gray-50" %>
  </div>
</div>