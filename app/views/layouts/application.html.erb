<!DOCTYPE html>
<<<<<<< HEAD
<html lang="ja">
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <meta name="format-detection" content="telephone=no"><!-- iOSのTEL自動リンク抑止 -->
  <title><%= content_for?(:title) ? yield(:title) : "ヘルスノ陣" %></title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= favicon_link_tag "favicon.png" %> <!-- 追加: /app/assets/images に設置 -->
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  <%= yield :head %> <!-- 追加: 画面ごとの追加タグ/スクリプト差し込み口 -->
</head>

<body class="min-h-[100dvh] flex flex-col bg-gray-50 text-gray-900
             pl-[env(safe-area-inset-left)] pr-[env(safe-area-inset-right)]">
  <a href="#main" class="sr-only focus:not-sr-only focus:absolute focus:top-2 focus:left-2
                        bg-white/90 px-3 py-2 rounded shadow">
    メインコンテンツへスキップ
  </a>

  <%= render "shared/header" %>

  <main id="main" class="container mx-auto max-w-screen-xl w-full flex-1 px-4 sm:px-6 lg:px-8 py-6">
    <%= render "shared/flash" if lookup_context.exists?("shared/flash") %> <!-- パス表記を修正 -->
    <%= yield %>
  </main>

  <%= render "shared/footer" %>

  <!-- iOSのホームインジケータ重なり対策（必要ならフッター側に移してもOK） -->
  <div class="h-[env(safe-area-inset-bottom)]"></div>
</body>
=======
<html>
  <head>
    <title><%= content_for(:title) || "Myapp" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body>
    <%= yield %>
  </body>
>>>>>>> origin/main
</html>
