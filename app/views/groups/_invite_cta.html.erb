<% return unless group.owner_id == current_user.id && group.invite_token.present? %>
<% url = invite_url(token: group.invite_token) %>

<div class="mt-10 text-center space-y-3">
  <h2 class="text-sm md:text-base font-semibold text-base-content/80">メンバーを招待しましょう！</h2>
  <p class="text-xs text-base-content/60">LINEなどでリンクを送るだけで参加できます。</p>

  <button
    type="button"
    class="btn btn-primary btn-sm"
    onclick="navigator.clipboard.writeText('<%= j url %>')
      .then(()=>{const t=this.textContent; this.textContent='コピーしました'; setTimeout(()=>this.textContent=t,1200);})
      .catch(()=>prompt('コピーできない場合は手動でコピーしてください', '<%= j url %>'))">
    招待リンクをコピー
  </button>
</div>
