require 'rails_helper'

RSpec.describe 'ExerciseLog', type: :model do
  describe 'バリデーション' do
    it '最小入力が揃っていれば有効' do
      pending 'base_points/必須項目の仕様が固まり次第、具体化する'
    end

    it '必須の1つが欠けると無効' do
      pending 'どの属性を必須にするかが決まり次第、実装する'
    end
  end
end